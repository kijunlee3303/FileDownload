<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
     <files-path>는 앱의 내부 저장소(context.filesDir)를 의미합니다.
     path="."는 이 폴더 아래의 모든 파일을 공유 대상으로 지정합니다.
    -->
    <files-path name="internal_storage" path="." />
</paths>
